import React from "react";
import { StyledMainContent } from "./MainContentStyle";

const MainContent = ({ children }) => {
	return <StyledMainContent>{children}</StyledMainContent>;
};

export default MainContent;
